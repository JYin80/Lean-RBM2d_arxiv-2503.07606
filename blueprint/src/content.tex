% Every node carrying \lean{}\leanok below must resolve to a declaration and
% survive a full `lake build`. The baseline of 2026-09-22 05:03 UTC passed
% 3446 build jobs with zero errors and the namespace axiom audit.
% Nodes without \leanok are open work orders in docs/TASKS.md.
%
% House rule: the coordinator adds \leanok in the integration commit after
% checking the corresponding declaration and full build. Never mark a node
% green merely because a worker's isolated module compiled.

\chapter{The model and the propagator}

\begin{definition}[Block covariance matrix, \S2.1]\label{def:SB}
  \lean{RBM.SB}\leanok
  Let $L\ge 3$ and work on the discrete torus $\ZL$.  Set
  \[ \SB_{ab} \;=\; \tfrac15\,\mathbf 1\!\left(|a-b|_L\le 1\right),\qquad a,b\in\ZL, \]
  the transition matrix of the lazy nearest-neighbour random walk on $\ZL$.
  Equivalently, $\SB$ is the circulant matrix on the group $\ZL$ generated by the
  kernel supported on $\{(0,0),(\pm1,0),(0,\pm1)\}$ with value $1/5$.
\end{definition}

\begin{remark}[Why $L\ge3$]\label{rem:L3}
  A standing hypothesis, not a restriction: every statement is asymptotic in
  $L\to\infty$.  It is not vacuous either --- for $L\le2$ the five lattice points
  above are not distinct, the row sums of $\SB$ are $1/5$ resp.\ $3/5$, and
  ``$S$ is stochastic'' is false as written.  The submitted version said only
  ``fix positive integers $W,L>0$''; it now fixes $L\ge3$ at the model definition.
  See \texttt{docs/paper-deltas.md}, item 1.
\end{remark}

\begin{definition}[Periodic distance]\label{def:dist}
  \lean{RBM.zdist2}\leanok
  $|x|_L$ denotes the periodic distance on $\ZL$.  The paper uses the periodic
  Euclidean norm and notes that the periodic $L^1$ and $L^2$ norms differ by at
  most $\sqrt2$, which is absorbed into the constants; we formalize the $L^1$
  one.  See \texttt{docs/paper-deltas.md}, item 2.
\end{definition}

\begin{lemma}[Elementary properties of $\SB$]\label{lem:SB-basic}
  \lean{RBM.SB_isSymm, RBM.SB_apply_add_right, RBM.sum_SB_row, RBM.SB_mulVec_one,
    RBM.card_sbSupport}\leanok
  \uses{def:SB}
  $\SB$ is symmetric and translation invariant, each row sums to $1$, and
  $\SB\mathbf 1=\mathbf 1$.
\end{lemma}
\begin{proof}\leanok
  Symmetry and translation invariance are immediate from the circulant form.  The
  row sums reduce, after the change of variables $b\mapsto a-b$, to the total mass
  of the kernel, which is $5\cdot\frac15=1$ because the five lattice points
  $(0,0),(\pm1,0),(0,\pm1)$ are distinct when $L\ge3$.
\end{proof}

\begin{definition}[The propagator, Definition \texttt{def\_Theta}]\label{def:Theta}
  \lean{RBM.Theta}\leanok\uses{def:SB}
  For $\xi\in\CC$ with $|\xi|<1$,
  \[ \Th^{(B)}_\xi := \frac{1}{1-\xi\,\SB}. \]
  In Lean this is \texttt{Ring.inverse} of $1-\xi\SB$ in the matrix ring with the
  $\ell^\infty$ operator norm; $\|\SB\|=1$ makes $1-\xi\SB$ a unit.
\end{definition}

\begin{lemma}[$\Th$ is the unique inverse]\label{lem:Theta-unique}
  \lean{RBM.Theta_mul, RBM.mul_Theta, RBM.eq_Theta_of_mul}\leanok\uses{def:Theta}
  $\Th^{(B)}_\xi$ is a two-sided inverse of $1-\xi\SB$, and any left inverse of
  $1-\xi\SB$ equals it.  This is the workhorse behind properties 1--3.
\end{lemma}
\begin{proof}\leanok
  $\SB$ is stochastic and symmetric, so $\|\SB\|=1$ in the $\ell^\infty$ operator
  norm; with $|\xi|<1$ this makes $1-\xi\SB$ a unit.  Uniqueness is the one-line
  computation $B=B\,(1-\xi\SB)\,\Th_\xi=\Th_\xi$.
\end{proof}

\begin{lemma}[Derivative of the block propagator, \texttt{(deri\_Thxi)}]
  \label{lem:Theta-deriv}
  \uses{def:Theta, lem:Theta-unique}
  \lean{RBM.Theta_sub_Theta, RBM.continuousAt_Theta,
        RBM.hasDerivAt_Theta_apply}\leanok
  For every block pair $a,b$, the complex derivative of
  $(\Th^{(B)}_\xi)_{ab}$ equals $(\Th^{(B)}_\xi\SB\Th^{(B)}_\xi)_{ab}$.
  This is the finite-dimensional, entrywise form of the paper's matrix identity.
  \emph{Work order T6, closed.}
\end{lemma}
\begin{proof}\leanok
  Subtract the inverses at two parameters, factor the resolvent identity, and
  pass to the limit using continuity of matrix inversion and evaluation.
\end{proof}

\begin{lemma}[Properties 1--4 of Lemma \texttt{lem\_propTH}]\label{lem:propTH-1234}
  \lean{RBM.Theta_transpose, RBM.Theta_apply_add_right, RBM.Theta_commute_SB,
    RBM.Theta_commute, RBM.Theta_eq_tsum, RBM.sum_Theta_row}\leanok
  \uses{def:Theta, lem:Theta-unique, lem:SB-basic}
  $\Th^{(B)}_\xi$ is symmetric and translation invariant, commutes with $\SB$ and
  with $\Th^{(B)}_{\xi'}$, and for $|\xi|<1$ admits the random-walk representation
  $\Th^{(B)}_\xi=\sum_{k\ge0}\xi^k(\SB)^k$.  Moreover
  $\sum_b(\Th^{(B)}_\xi)_{ab}=(1-\xi)^{-1}$.
\end{lemma}
\begin{proof}\leanok
  Properties 1--3 each transfer from the corresponding property of $\SB$ by
  uniqueness of the inverse: transpose, conjugate by the translation
  $a\mapsto a+s$, and commute.  Property 4 is the Neumann series, convergent in
  operator norm because $\|\xi\SB\|<1$.  The row sum follows from
  $\SB\mathbf1=\mathbf1$.
\end{proof}

\begin{lemma}[Crude bounds]\label{lem:crude}
  \lean{RBM.norm_Theta_le, RBM.sum_norm_Theta_row_le, RBM.norm_Theta_apply_le}\leanok
  \uses{lem:propTH-1234}
  $\max_a\sum_b|(\Th^{(B)}_\xi)_{ab}|\le(1-|\xi|)^{-1}$.  This is \emph{not} the
  sharp bound: the decay length here is governed by $1-|\xi|$, whereas the paper
  needs $\lhat(\xi)=\min(|1-\xi|^{-1/2},L)$.  The two differ enormously when
  $\xi=tm^2$.
\end{lemma}
\begin{proof}\leanok
  Bound the Neumann series term by term in the $\ell^\infty$ operator norm.
\end{proof}

\chapter{Section 8: proof of Lemma \texorpdfstring{\texttt{lem\_propTH}}{lem-propTH}}

\begin{definition}[Deterministic domination, Definition \texttt{stoch\_domination} (ii)]
  \label{def:detdom}
  \lean{RBM.UnifDetDom, RBM.DetDom}\leanok
  $A\prec B$ means that for every fixed $\varepsilon>0$ one has
  $A\le C_\varepsilon L^{\varepsilon}B$ for all large $L$; in particular powers of
  $\log L$ are harmless.  The sequence parameter is the number of blocks $L$,
  which is the form \S8 uses; the introduction's $N^{\tau}$ version follows since
  $L\le N$.  The basic notion is the version uniform in a parameter
  $u\in U(L)$, because Properties 5 and 6 hold uniformly in the lattice points.
  See \texttt{docs/paper-deltas.md}, items 3 and 4.
\end{definition}


\begin{definition}[Scales, \texttt{(eq\_kappa\_def)}]\label{def:kappa}
  \lean{RBM.kappa, RBM.ellhat}\leanok
  $\kappa:=|1-\xi|^{1/2}$ and $\lhat=\lhat(\xi):=\min(\kappa^{-1},L)$.
\end{definition}

\begin{lemma}[Elementary properties of $\kappa$ and $\lhat$]\label{lem:kappa-basic}
  \lean{RBM.kappa_sq, RBM.kappa_pos, RBM.kappa_mul_ellhat_le_one}\leanok\uses{def:kappa}
  $\kappa^2=|1-\xi|$, $\kappa>0$ for $|\xi|<1$, and $\kappa\lhat\le1$.
\end{lemma}
\begin{proof}\leanok
  $\kappa>0$ because $|\xi|<1$ forces $\xi\ne1$, and
  $\kappa\lhat\le\kappa\cdot\kappa^{-1}=1$.
\end{proof}

\section{Fourier setup}

\begin{definition}[The symbol, \texttt{(eq\_symbol)}]\label{def:Shat}
  \lean{RBM.Shat}\leanok\uses{def:SB}
  For $p\in\TL$,
  $\Shat(p)=\frac{1+2\cos p_1+2\cos p_2}{5}$.
\end{definition}

\begin{lemma}[Plane waves diagonalize $\SB$]\label{lem:eigen}
  \lean{RBM.Shat_eq_cos, RBM.SB_mulVec_char, RBM.norm_Shat_le_one,
    RBM.one_sub_mul_Shat_ne_zero}\leanok
  \uses{def:Shat, def:SB}
  $\SB e_p=\Shat(p)e_p$, $|\Shat(p)|\le1$, and $1-\xi\Shat(p)\ne0$ for $|\xi|<1$.
\end{lemma}
\begin{proof}\leanok
  Expanding $\SB$ over its five-point support turns each shift into a character
  value, which is exactly $\Shat(p)$.  Then $|\Shat(p)|\le1$ gives
  $|\xi\Shat(p)|<1$, so $1-\xi\Shat(p)\ne0$.
\end{proof}

\begin{lemma}[Fourier representation, \texttt{(eq\_Fourier\_rep)}]\label{lem:fourier}
  \lean{RBM.Theta_apply_fourier, RBM.inv_mul_sum_chr}\leanok
  \uses{lem:eigen, lem:Theta-unique}
  With $x=a-b$,
  \[ (\Th^{(B)}_\xi)_{ab}=K_{\xi,L}(x)=\frac1{L^2}\sum_{p\in\TL}
     \frac{e^{\mathrm{i}p\cdot x}}{1-\xi\Shat(p)} . \]
\end{lemma}
\begin{proof}\leanok
  Character orthogonality on $\ZL$ shows that the kernel $K_{\xi,L}$ solves
  $K-\xi\SB K=\delta_0$; since $\Th_\xi$ is the unique inverse of $1-\xi\SB$, the
  circulant matrix generated by $K_{\xi,L}$ is $\Th_\xi$.
\end{proof}

\begin{definition}[The symbol gap $q(p)$, \texttt{(eq\_qdef)}]\label{def:q}
  \lean{RBM.qsym}\leanok\uses{def:Shat}
  $q(p):=1-\Shat(p)=\frac25\big[(1-\cos p_1)+(1-\cos p_2)\big]$.
\end{definition}

\begin{lemma}[Ellipticity, \texttt{(eq\_elliptic)}]\label{lem:elliptic}
  \lean{RBM.norm_one_sub_mul_Shat_le, RBM.norm_one_sub_mul_Shat_ge,
    RBM.norm_one_sub_mul_Shat_ge_kappa}\leanok
  \uses{def:q, def:kappa}
  For $|\xi|<1$,
  \[ \tfrac19\big(\kappa^2+q(p)\big)\;\le\;\big|1-\xi\Shat(p)\big|\;\le\;\kappa^2+q(p). \]
  The paper's proof splits at $\Shat(p)=1/2$; in the low branch its constant
  $1/2$ should read $2/5$, see \texttt{docs/paper-deltas.md}, item 5.
\end{lemma}
\begin{proof}\leanok
  Write $\lambda=\Shat(p)$, which is real and lies in $[-3/5,1]$.  If
  $\lambda\le1/2$ the multiplier is bounded away from $1$ outright.  If
  $\lambda\ge1/2$, use $1-\lambda\xi=(1-\lambda)+\lambda(1-\xi)$: since
  $|\xi|<1$ we have $\operatorname{Re}(1-\xi)>0$, so the real parts of the two
  summands cannot cancel and $|1-\lambda\xi|$ dominates both
  $(1-\lambda)+\lambda\operatorname{Re}(1-\xi)$ and
  $\lambda|\operatorname{Im}(1-\xi)|$.  The upper bound is the triangle
  inequality and needs no hypothesis on $\xi$ at all.
\end{proof}

\begin{definition}[$|p|_*$ on the lattice]\label{def:pstar}
  \uses{def:dist}
  \lean{RBM.pstar, RBM.pstar2}\leanok
  On $\TL$ the frequency in coordinate $i$ is $2\pi p_i/L$, and there
  $\operatorname{dist}(2\pi p_i/L,2\pi\mathbb Z)=2\pi\,\mathrm{zdist}(p_i)/L$
  \emph{exactly}.  So $|p|_*$ is defined straight from the cycle distance and
  $\operatorname{dist}(\cdot,2\pi\mathbb Z)$ is never formalized; the single
  identity carrying the periodicity is \texttt{RBM.cos\_eq\_cos\_pstar}.
\end{definition}

\begin{lemma}[$q(p)\sim|p|_*^2$, \texttt{(eq\_qcomp)}]\label{lem:qcomp}
  \uses{def:q, def:pstar}
  \lean{RBM.qsym_le_pstar2, RBM.pstar2_le_qsym,
        RBM.norm_one_sub_mul_Shat_le_pstar, RBM.norm_one_sub_mul_Shat_ge_pstar}\leanok
  $q(p)\sim|p|_*^2$, where $|p|_*^2=\operatorname{dist}(p_1,2\pi\mathbb Z)^2
  +\operatorname{dist}(p_2,2\pi\mathbb Z)^2$; equivalently
  $|1-\xi\widehat S(p)|\sim\kappa^2+|p|_*^2$, which is the form \S8.2 and \S8.3 use.
  Explicitly $\tfrac{4}{5\pi^2}|p|_*^2\le q(p)\le\tfrac15|p|_*^2$.
\end{lemma}
\begin{proof}\leanok
  By \texttt{RBM.cos\_eq\_cos\_pstar} the cosine does not see the difference
  between $2\pi p_i/L$ and its distance to $2\pi\mathbb Z$, so each coordinate
  reduces to the elementary two-sided bound
  $\tfrac{2}{\pi^2}\theta^2\le1-\cos\theta\le\tfrac12\theta^2$ on $|\theta|\le\pi$,
  the hypothesis being supplied by $\mathrm{zdist}\le L/2$.  Merging with
  \eqref{lem:elliptic} costs only the numeric fact $4/(45\pi^2)\le1/9$, i.e.\ $\pi>3$.
\end{proof}

\begin{lemma}[Shell counting on $\TL$]\label{lem:shells}
  \uses{def:dist}
  \lean{RBM.shellIndex, RBM.card_filter_zdist_eq_le, RBM.card_filter_zdist_le_le,
        RBM.card_shell_le, RBM.sum_erase_zero_eq_sum_shells}\leanok
  With $k(p)=\max(\mathrm{zdist}(p_1),\mathrm{zdist}(p_2))$ one has
  $\#\{p\in\TL: k(p)=k\}\le12k+4$, and every sum over $\TL\setminus\{0\}$
  splits into the shells $k=1,\dots,L$.  Pure \texttt{Finset} accounting: no reals.
\end{lemma}
\begin{proof}\leanok
  A point of the shell has one coordinate at periodic distance exactly $k$ and
  the other at distance at most $k$, so the shell is covered by two products
  sphere $\times$ ball.  The sphere has at most two points because
  $\min(u.\mathrm{val},L-u.\mathrm{val})=k$ pins $u.\mathrm{val}$ to $k$ or to
  $L-k$; the ball has at most $2k+1$ because its points are the representatives
  $0,\dots,k$ together with $-1,\dots,-k$.  Lean keeps the looser bound
  $\le12k+4$.  The fibrewise
  rewriting is \texttt{Finset.sum\_fiberwise\_of\_maps\_to}, whose hypothesis is
  exactly $k(p)\in[1,L]$ for $p\ne0$.
  \emph{Work order T15, closed.}
\end{proof}

\begin{lemma}[Harmonic sum]\label{lem:harmonic}
  \uses{def:detdom}
  \lean{RBM.mul_log_le_rpow, RBM.sum_inv_Icc_le_one_add_log,
        RBM.one_add_log_detDom_one, RBM.harmonic_detDom_one}\leanok
  $\sum_{k=1}^{L}k^{-1}\le1+\log L\prec1$, the Lean form of the paper's remark
  that powers of $\log L$ are harmless.
\end{lemma}
\begin{proof}\leanok
  Mathlib has $\sum_{k=1}^{n}k^{-1}\le1+\log n$ over $\mathbb Q$
  (\texttt{harmonic\_le\_one\_add\_log}), so the first inequality is a cast.
  For the domination, $\log y\le y-1$ at $y=x^{\tau}$ together with
  $\log(x^{\tau})=\tau\log x$ gives $\tau\log x\le x^{\tau}$; keeping that
  multiplicative rather than dividing by $\tau$ avoids every division lemma.
  Splitting $L^{\tau}=L^{\tau/2}\cdot L^{\tau/2}$ and using that $L^{\tau/2}$
  eventually exceeds the constant $1+(\tau/2)^{-1}$ then gives
  $1+\log L\le L^{\tau}$ for all large $L$, which is the definition of $\prec1$.
\end{proof}

\begin{lemma}[Lattice sum]\label{lem:latticesum}
  \uses{lem:qcomp, lem:shells, lem:harmonic}
  \lean{RBM.sum_inv_pstar2_le, RBM.normalized_sum_inv_pstar2_le_log,
        RBM.normalizedPuncturedSum_detDom_one}\leanok
  $\sum_{p\in\TL\setminus\{0\}}|p|_*^{-2}
   \le(4/\pi^2)L^2\sum_{k=1}^L k^{-1}$; after division by $L^2$ this
  is $O(\log L)\prec1$.  The sequence is extended by zero for $L<3$.
\end{lemma}
\begin{proof}\leanok
  On the shell $k\ge1$, $|p|_*^2\ge(2\pi k/L)^2$ and the proved count
  $12k+4\le16k$ yields at most $4L^2/(\pi^2 k)$.  Sum over shells and use
  the harmonic bound.  A compiled $L=3$ example shows the punctured sum is
  strictly positive, so the statement is not vacuous.
  \emph{Work order T3, closed.}
\end{proof}

\begin{lemma}[Separating the zero mode]\label{lem:zero-mode}
  \uses{lem:fourier}
  \lean{RBM.Shat_zero, RBM.chr_zero_left, RBM.norm_chr,
        RBM.Theta_apply_eq_zero_mode_add, RBM.Theta_apply_sub_eq_erase_sum}\leanok
  In \texttt{(eq\_Fourier\_rep)} the $p=0$ term is $L^{-2}(1-\xi)^{-1}$, because
  $\hat S(0)=1$ and $e_0\equiv1$; in a difference $K_{\xi,L}(x)-K_{\xi,L}(y)$ it
  cancels outright.  Both Property 5 and Property 6 begin with exactly this step,
  so it is proved once.  The companion $\|e_p(u)\|=1$ lives here too, so that
  neither of the two consumers has to re-derive it.
\end{lemma}
\begin{proof}\leanok
  $\hat S(0)=1$ is the five-point average of $e_0\equiv1$, so the $p=0$ summand of
  \texttt{(eq\_Fourier\_rep)} is $L^{-2}(1-\xi)^{-1}$ and the rest is a sum over
  $\TL\setminus\{0\}$ (\texttt{Theta\_apply\_eq\_zero\_mode\_add}).  Subtracting two
  such expansions cancels it (\texttt{Theta\_apply\_sub\_eq\_erase\_sum}); in Lean the
  cancellation is \texttt{Finset.sum\_erase} rather than a limit, which is why the
  whole step stays algebraic.  $\|e_p(u)\|=1$ comes from
  \texttt{ZMod.stdAddChar} being unitary.
  \emph{Work order T14, closed by CI \#25.}
\end{proof}

\section{Property 5: exponential decay}

\begin{lemma}[Decay in the regime $\kappa L<1$]\label{lem:decay-small}
  \uses{lem:fourier, lem:elliptic, lem:latticesum, lem:zero-mode}
  \lean{RBM.norm_Theta_apply_le_small_kappa_log,
        RBM.small_kappa_prefactor_detDom_one,
        RBM.ellhat_eq_L_of_kappa_mul_L_lt_one}\leanok
  If $\kappa L<1$, so that $\lhat=L$, then separating the zero mode gives
  $|K_{\xi,L}(x)|\le\kappa^{-2}L^{-2}+C\log L\prec\kappa^{-2}L^{-2}$, which is
  \texttt{(prop:ThfadC)} because $|x|_L\le CL=C\lhat$.
  \emph{Work order T4, closed.}
\end{lemma}
\begin{proof}\leanok
  The zero-mode decomposition and elliptic denominator bound reduce the
  punctured sum to T3's two-dimensional harmonic bound.  Since
  $\kappa L<1$, its logarithmic correction is subpolynomial relative to the
  zero-mode scale.  Also $|x|_L\le L=\lhat$, so the exponential factor costs
  at most a fixed factor $e$.
\end{proof}

\begin{lemma}[Two-dimensional logarithmic integral, \texttt{(eq\_log\_int)}]
  \label{lem:log-integral}
  \lean{RBM.LogIntegral.log_integral_le,
        RBM.LogIntegral.log_integral_square_le}\leanok
  For every $\kappa>0$, the actual product-Lebesgue integral on the square is
  bounded by $32\log(2+\kappa^{-1})$.  On $[-\pi,\pi]^2$ the periodic
  momentum norm agrees with the Euclidean norm used in the Lean statement.
  \emph{Work order T25, closed.}
\end{lemma}
\begin{proof}\leanok
  Bound the first coordinate integral by an arctangent slice, then integrate
  $(\kappa+|t|)^{-1}$ to obtain the logarithm.  Fubini converts the iterated
  integral to the paper's square integral.
\end{proof}

\begin{definition}[Continuous symbol and infinite-volume kernel, \texttt{(eq\_Kinf)}]
  \label{def:continuum}
  \uses{lem:elliptic, lem:qcomp}
  \lean{RBM.ScontReal, RBM.qcont, RBM.Dcont,
        RBM.norm_Dcont_ge_pnorm2, RBM.norm_Dcont_le_pnorm2,
        RBM.Kinf, RBM.KinfIntegrand_integrableOn,
        RBM.Kinf_eq_square_integral, RBM.Kinf_zero_zero}\leanok
  The continuous counterpart of $\widehat S$ obeys the two-sided elliptic
  bound on $[-\pi,\pi]^2$.  The infinite-volume Fourier kernel is defined as
  a genuine integrable function; Fubini identifies its iterated and square
  integrals.  At $\xi=0$ and $x=0$ the normalized kernel equals $1$.
  \emph{Work order T26, closed.}
\end{definition}

\begin{lemma}[Infinite-volume kernel and the contour shift]\label{lem:contour}
  \uses{def:continuum, lem:contour-first}
  $|D_\xi(p+\mathrm{i}\eta)|\ge c(\kappa^2+|p|_*^2)$ for $|\eta|\le c_0\kappa$, and
  hence $|K_{\xi,\infty}(x)|\le C\log(2+\kappa^{-1})e^{-c\kappa|x|}$.
  The first-coordinate Cauchy shift is proved below; its shifted-integral
  estimate and the second-coordinate step remain open. \emph{Work order T27.}
\end{lemma}

\begin{lemma}[First-coordinate zero-free strip and Cauchy shift]\label{lem:contour-first}
  \uses{def:continuum}
  \lean{RBM.contourWidth, RBM.norm_Dfirst_shift_ge,
        RBM.contourIntegrand_differentiableOn,
        RBM.contour_integral_shift_first}\leanok
  For $|\eta|\le\kappa/10000$ and real frequencies in the Fourier square,
  the actual denominator is zero-free after shifting the first coordinate.
  Holomorphy, cancellation of the vertical sides, and Cauchy's theorem give
  equality of the original and shifted first-coordinate integrals.
  \emph{T27 intermediate result.}
\end{lemma}
\begin{proof}\leanok
  Bound the change of the cosine symbol by the quadratic and linear terms in
  the strip width, then apply the rectangular contour theorem to the periodic
  integrand. The integer spatial coordinate cancels the vertical sides.
\end{proof}

\begin{lemma}[Periodization]\label{lem:periodize}
  \uses{def:continuum, lem:log-integral, lem:fourier,
         lem:periodize-shells, lem:periodize-tail,
         lem:periodize-convergence, lem:periodize-shift,
         lem:periodize-descend, lem:periodize-stencil,
         lem:periodize-delta, lem:periodize-torus-stencil,
         lem:periodize-resolvent-bridge, lem:periodize-fourier-delta,
         lem:periodize-integrand}
  $K_{\xi,L}(x)=\sum_{n\in\mathbb Z^2}K_{\xi,\infty}(x+nL)$, by showing that the
  periodized kernel solves $(1-\xi S)P=\delta_0$ and using uniqueness of the
  inverse. Shell counting, the conditional bridge, and the actual integer
  lattice resolvent equation are proved below. A spatial decay estimate for
  the actual kernel is still required to justify every shifted lattice sum.
  This does not depend on T27's contour estimate except for that decay input.
  \emph{Work order T28.}
\end{lemma}

\begin{lemma}[Integer-square shells for periodization]\label{lem:periodize-shells}
  \lean{RBM.periodizationBox, RBM.periodizationShell,
        RBM.card_periodizationBox, RBM.existsUnique_mem_periodizationShell,
        RBM.card_periodizationShell_pos, RBM.card_periodizationShell_le}\leanok
  In $\mathbb Z^2$ the square of max-radius $r$ has $(2r+1)^2$ points, and the
  positive-radius shell has exactly $8r$. Every lattice point belongs to a
  unique shell. \emph{T28a finite-counting step, closed.}
\end{lemma}
\begin{proof}\leanok
  Use the integer interval cardinality and Mathlib's two-dimensional
  box-shell partition.
\end{proof}

\begin{lemma}[Summable geometric tails on $\mathbb Z^2$]
  \label{lem:periodize-tail}
  \uses{lem:periodize-shells}
  \lean{RBM.sum_periodizationShell_pow, RBM.summable_shell_weights,
        RBM.summable_latticeMaxRadius_pow}\leanok
  For $0\le\rho<1$, the weight $\rho^{\|n\|_\infty}$ is summable over
  $n\in\mathbb Z^2$. This is the scalar convergence ingredient for T28;
  a matching bound for the actual $K_{\xi,\infty}$ is still required.
  \emph{T28b, closed.}
\end{lemma}
\begin{proof}\leanok
  Partition $\mathbb Z^2$ into max-radius shells and sum the exact $8r$
  multiplicity against a geometric factor.
\end{proof}

\begin{lemma}[Complex kernels under a geometric majorant]
  \label{lem:periodize-convergence}
  \uses{lem:periodize-tail}
  \lean{RBM.summable_complex_lattice_of_geometric_bound,
        RBM.summable_complex_lattice_translate_of_geometric_bound}
  \leanok
  A complex kernel on $\mathbb Z^2$ is summable if its norm is bounded by
  $C\rho^{\|n\|_\infty}$ with $C\ge0$ and $0\le\rho<1$; the same holds for
  a translated kernel with such a bound. This comparison criterion does not
  itself establish decay for $K_{\xi,\infty}$. \emph{T28c, closed.}
\end{lemma}
\begin{proof}\leanok
  Apply the summable scalar majorant from the shell calculation.
\end{proof}

\begin{lemma}[Lattice-shift invariance of periodization]
  \label{lem:periodize-shift}
  \uses{lem:periodize-convergence}
  \lean{RBM.periodizeKernel, RBM.periodizeKernel_add_smul}\leanok
  The formal sum $\sum_{n\in\mathbb Z^2}K(x+Ln)$ is unchanged when $x$ is
  replaced by $x+Lk$. This is the algebraic periodicity step; the actual
  kernel's convergence and resolvent equation remain separate.
  \emph{T28d, closed.}
\end{lemma}
\begin{proof}\leanok
  Reindex the sum by translation on $\mathbb Z^2$.
\end{proof}

\begin{lemma}[Periodization descends to the discrete torus]
  \label{lem:periodize-descend}
  \uses{lem:periodize-shift}
  \lean{RBM.torusLatticeLift, RBM.periodizedTorusKernel,
        RBM.periodizedTorusKernel_eq_of_cast}\leanok
  The periodized kernel gives a well-defined function on $\mathbb Z_L^2$:
  evaluating it at any integer representative of a torus point gives the
  same value. Actual $K_{\xi,\infty}$ summability and the resolvent equation
  remain open. \emph{T28e, closed.}
\end{lemma}
\begin{proof}\leanok
  Coordinatewise equality modulo $L$ yields an $L\mathbb Z^2$ shift, then
  apply periodization invariance.
\end{proof}

\begin{lemma}[Five-point stencil commutes with periodization]
  \label{lem:periodize-stencil}
  \uses{lem:periodize-descend, lem:periodize-convergence}
  \lean{RBM.latticeFivePoint, RBM.latticeFiveAverage,
        RBM.periodizeKernel_latticeFiveAverage}\leanok
  Under explicit summability of each of the five shifted kernel families,
  periodization commutes with the origin-and-four-neighbours average.
  A finitely supported point mass gives a nonzero example. The actual
  infinite-volume Green kernel's summability and resolvent equation remain
  open. \emph{T28f, closed.}
\end{lemma}
\begin{proof}\leanok
  Move the finite stencil sum through the absolutely convergent lattice sum.
\end{proof}

\begin{lemma}[Point mass periodizes to the torus delta]
  \label{lem:periodize-delta}
  \uses{lem:periodize-descend, lem:periodize-stencil}
  \lean{RBM.torusLatticeLift_add_smul_eq_zero_iff,
        RBM.periodizedTorusKernel_latticePointMass}\leanok
  For positive $L$, periodizing the point mass at the origin of $\mathbb Z^2$
  gives the Kronecker delta at the origin of $\mathbb Z_L^2$. This is the
  right-hand side of the intended periodized resolvent equation.
  \emph{T28g, closed.}
\end{lemma}
\begin{proof}\leanok
  The canonical representative $x+Ln$ is zero precisely when $x=0$ and $n=0$.
\end{proof}

\begin{lemma}[Periodized stencil equals the torus walk]
  \label{lem:periodize-torus-stencil}
  \uses{lem:periodize-stencil, lem:periodize-descend, def:SB}
  \lean{RBM.periodizedTorusKernel_latticeFiveAverage,
        RBM.summable_latticePointMass_periodized}\leanok
  For the symmetric five-point walk and explicitly summable translates,
  periodization of the integer-lattice stencil equals the action of $S^{(B)}$
  on the descended torus kernel. The point mass at $L=3$ gives a nonzero
  example. The actual infinite-volume resolvent equation remains open.
  \emph{T28h, closed.}
\end{lemma}
\begin{proof}\leanok
  Match the five shifted canonical representatives with the torus neighbours,
  then use finite-stencil summation and representative independence.
\end{proof}

\begin{lemma}[Conditional periodized resolvent bridge]
  \label{lem:periodize-resolvent-bridge}
  \uses{lem:periodize-torus-stencil, lem:periodize-delta, def:Theta}
  \lean{RBM.Theta_eq_circulant_periodized,
        RBM.Theta_apply_periodized}\leanok
  If an integer-lattice kernel satisfies the genuine five-point resolvent
  equation and all five shifted lattice sums converge, its torus
  periodization equals the finite-volume $\Theta_\xi$ kernel when
  $\|\xi\|<1$. A nonzero point-mass example at $\xi=0$ satisfies the
  hypotheses. The actual $K_{\xi,\infty}$ must still be shown to satisfy
  them. \emph{T28 conditional bridge, closed.}
\end{lemma}
\begin{proof}\leanok
  Periodize the lattice equation, identify the torus five-point stencil and
  delta, then use uniqueness of the finite-volume inverse.
\end{proof}

\begin{lemma}[Two-dimensional Fourier inversion of the numerator]
  \label{lem:periodize-fourier-delta}
  \uses{def:continuum}
  \lean{RBM.integral_exp_int_periodize,
        RBM.normalized_integral_continuumNumerator}\leanok
  The normalized iterated integral of the numerator of the infinite-volume
  Fourier kernel over the two-dimensional frequency square equals the point
  mass at the origin of $\mathbb Z^2$. This is the right-hand side of the
  actual lattice resolvent equation; the left-hand side still requires an
  integrand identity and passage through the integral.
  \emph{T28j, closed.}
\end{lemma}
\begin{proof}\leanok
  Factor the exponential into its two integer-frequency characters and use
  the one-dimensional Fourier orthogonality integral in each coordinate.
\end{proof}

\begin{lemma}[Actual infinite-volume lattice resolvent equation]
  \label{lem:periodize-integrand}
  \uses{def:continuum, lem:periodize-fourier-delta}
  \lean{RBM.latticeFiveAverage_continuumNumerator,
        RBM.KinfIntegrand_sub_latticeFiveAverage,
        RBM.Kinf_sub_latticeFiveAverage}\leanok
  For $\|\xi\|<1$, the actual Fourier kernel satisfies
  $K_{\xi,\infty}-\xi S_\infty K_{\xi,\infty}=\delta_0$ pointwise on
  $\mathbb Z^2$. The proof includes the exact five-point symbol identity
  and interchange of the finite stencil with both frequency integrals.
  Spatial decay and shifted lattice summability remain open.
  \emph{T28 actual lattice equation, closed.}
\end{lemma}
\begin{proof}\leanok
  Cancel the Fourier denominator in the five-point integrand identity,
  integrate termwise, and use two-dimensional Fourier orthogonality.
\end{proof}

\begin{lemma}[Combining the two decay regimes]\label{lem:decay-interface}
  \uses{lem:decay-small, lem:contour, lem:periodize, lem:log-integral,
         lem:kappa-basic}
  Assemble Property~5 from the proved small-$\kappa L$ estimate and the
  large-$\kappa L$ estimate obtained by contour shift and periodization.
  This node remains open until those analytic bounds are actual Lean theorems;
  no freely supplied proof field represents the missing regime.
  \emph{Work order T24 after T27/T28.}
\end{lemma}

\begin{lemma}[Property 5, \texttt{(prop:ThfadC)}]\label{lem:propTH-5}
  \uses{lem:decay-small, lem:contour, lem:periodize, lem:decay-interface}
  $(\Th^{(B)}_\xi)_{ab}\prec e^{-c|a-b|_L/\lhat(\xi)}\big/\big(|1-\xi|\lhat(\xi)^2\big)$.
\end{lemma}

\section{Property 6: derivative bounds}

\begin{lemma}[Case 2: $|s|_L>d/2$]\label{lem:bd-case2}
  \uses{lem:fourier, lem:elliptic, lem:latticesum, lem:zero-mode}
  \lean{RBM.norm_Theta_fd_case2_le_paper,
        RBM.case2_log_prefactor_detDom_one}\leanok
  Both differences satisfy the two exact paper-scale bounds with common
  deterministic prefactor $720(1+\log L)\prec1$ whenever $L\ge3$,
  $\|\xi\|<1$ and $d<2|s|_L$.  The ratios $|s|_L/(d+1)$ and
  $|s|_L^2/(d^2+1)$ are bounded below by $1/2$ and $1/4$.
  \emph{Work order T5, closed.}
\end{lemma}
\begin{proof}\leanok
  The zero mode cancels.  Ellipticity bounds the remaining terms by the
  punctured inverse-square lattice sum, and the Case 2 geometry absorbs the
  resulting $O(\log L)$ into each paper-scale ratio.
\end{proof}

\begin{lemma}[The two dyadic sums]\label{lem:dyadic-sums}
  \uses{lem:kappa-basic}
  \lean{RBM.dyad, RBM.sum_dyad_pow_le, RBM.sum_pow_le_two_mul,
        RBM.dyadic_sum_three_le, RBM.dyadic_sum_four_le}\leanok
  \texttt{(eq\_dyadic\_sum1)} and \texttt{(eq\_dyadic\_sum2)}: summing the dyadic
  scales $r=2^{-j}$ against $r^{3}(\kappa^{2}+r^{2})^{-1}(1+rd)^{-3}$, resp.\ its
  $r^{4}$ analogue, gives $O((d+1)^{-1})$, resp.\ $O((d^{2}+1)^{-1})$.
\end{lemma}
\begin{proof}\leanok
  Everything reduces to one fact: a geometric sum over an \emph{arbitrary} set of
  scales is at most twice its largest term, in the decreasing direction
  (\texttt{sum\_dyad\_pow\_le\_two\_mul}) and, with ratio $b\ge2$, in the
  increasing one (\texttt{sum\_pow\_le\_two\_mul}).  Having both means the case
  analysis can use the filter condition directly and never has to name the index
  where the split happens.  One split suffices: on $r\le(d+1)^{-1}$ the factor
  $(1+rd)^{-3}\le1$ and the sum is $\le2(d+1)^{-1}$; on $r>(d+1)^{-1}$ the
  inequality $1+rd\ge r(d+1)$ --- which holds precisely because $r\le1$ ---
  turns the summand into $r^{-2}(d+1)^{-3}$, and the increasing sum is
  $\le2(d+1)^{2}(d+1)^{-3}$.  Both halves give $2(d+1)^{-1}$.
  \emph{Work order T17, closed.}
\end{proof}

\begin{lemma}[Case 1 of the derivative bounds, given \texttt{(eq\_dyadic)}]\label{lem:dyadic-case1}
  \uses{lem:dyadic-sums, lem:zero-mode, lem:kappa-basic}
  \lean{RBM.DyadicDecomp, RBM.norm_Theta_fd1_le, RBM.norm_Theta_fd2_le,
        RBM.norm_Theta_fd1_le_paper, RBM.norm_Theta_fd2_le_paper}\leanok
  Carrying \texttt{(eq\_dyadic)} as the interface \texttt{DyadicDecomp} --- a
  \texttt{structure} field, never an \texttt{axiom} --- Case 1 of
  \texttt{(prop:BD1)} and \texttt{(prop:BD2)} is already a theorem: sum the
  per-annulus estimate against the two dyadic sums.
\end{lemma}
\begin{proof}\leanok
  The zero mode cancels in both differences, so the difference is the sum over
  annuli; bound each by the interface and sum with \texttt{lem:dyadic-sums}.
  The paper's second right-hand term is recovered from $\kappa\lhat\le1$.
  \emph{Work order T18, closed.}
\end{proof}

\begin{lemma}[Explicit dyadic cutoff]\label{lem:cutoff}
  \lean{RBM.contDiff_dyadicCutoff_three, RBM.sum_dyadicCutoff_eq_one,
        RBM.dyadicCutoff_support, RBM.dyadicCutoff_nonneg,
        RBM.abs_dyadicCutoff_thirdForwardDiff_le}\leanok
  A scale-uniform third finite-difference bound for an explicit polynomial
  cutoff, together with an exact partition on the normalized interval
  $2^{-J}\le t\le1$. T22 must normalize its frequency and cover the smallest
  nonzero mode when choosing $J$. \emph{Work order T19, closed.}
\end{lemma}
\begin{proof}\leanok
  Use a degree-seven smoothstep with three flat derivatives at both endpoints;
  the dyadic differences telescope, and the third derivative bounds the third
  forward difference.
\end{proof}

\begin{lemma}[Periodic summation by parts]\label{lem:abel}
  \lean{RBM.sum_fwdDiff_mul, RBM.norm_stdAddChar_sub_one_sq,
        RBM.chr_e1_distance_bounds, RBM.chr_e2_distance_bounds}\leanok
  Reindex finite sums on $\ZL$ to transfer lattice differences from a
  multiplier to a character, with no boundary term. The phase loss in each
  coordinate is quantitatively comparable to its periodic graph distance.
  \emph{Work order T20, closed.}
\end{lemma}
\begin{proof}\leanok
  Reindex by translation on the finite torus and apply the exact complex
  exponential phase identity, followed by the half-angle trigonometric bounds.
\end{proof}

\begin{lemma}[Multiplier differences on an annulus]\label{lem:symbol-diff}
  \uses{lem:elliptic, lem:qcomp, lem:symbol-diff-shat,
         lem:symbol-diff-reciprocal-one,
         lem:symbol-diff-reciprocal-two,
         lem:symbol-diff-reciprocal-two-bound,
         lem:symbol-denom-annulus,
         lem:symbol-shift-annulus,
         lem:symbol-reciprocal-annulus-bound}
  Bound the first three lattice differences of the inverse symbol uniformly
  on a dyadic annulus, retaining the gain from $|\sin p_i|\lesssim|p|_*$.
  The first three differences of $\widehat S$ itself are proved below;
  the first reciprocal quotient is proved below. Higher reciprocal and
  shifted-annulus bounds remain open. \emph{Work order T21.}
\end{lemma}

\begin{lemma}[First three coordinate differences of $\widehat S$]
  \label{lem:symbol-diff-shat}
  \uses{lem:abel, lem:fourier}
  \lean{RBM.norm_Shat_shift_e1_le, RBM.norm_Shat_shift_e2_le,
        RBM.norm_Shat_second_diff_e1_le, RBM.norm_Shat_second_diff_e2_le,
        RBM.norm_Shat_third_diff_e1_le, RBM.norm_Shat_third_diff_e2_le}\leanok
  Each first coordinate difference costs $O(|p_i|_*/L+L^{-2})$;
  second and third coordinate differences cost $O(L^{-2})$ and $O(L^{-3})$.
  These are the actual two-dimensional Fourier symbol, not an abstract
  smoothness hypothesis. \emph{T21 intermediate result.}
\end{lemma}
\begin{proof}\leanok
  Factor the positive and negative Fourier characters exactly, then bound
  each step factor by the phase estimates from periodic summation by parts.
\end{proof}

\begin{lemma}[First difference of the reciprocal Fourier multiplier]
  \label{lem:symbol-diff-reciprocal-one}
  \uses{lem:symbol-diff-shat, lem:elliptic}
  \lean{RBM.invSymbolMultiplier,
        RBM.invSymbolMultiplier_sub,
        RBM.norm_invSymbolMultiplier_shift_e1_le,
        RBM.norm_invSymbolMultiplier_shift_e2_le}\leanok
  The first difference of $(1-\xi\widehat S)^{-1}$ has the exact quotient
  $\xi(\widehat S(p)-\widehat S(q))/
  [(1-\xi\widehat S(p))(1-\xi\widehat S(q))]$.
  Both coordinate norm bounds retain the precise denominator product;
  annular denominator comparisons remain open. \emph{T21a, closed.}
\end{lemma}
\begin{proof}\leanok
  Subtract the two nonzero reciprocal factors and insert the proved
  coordinate differences of $\widehat S$.
\end{proof}

\begin{lemma}[Exact second difference of the reciprocal multiplier]
  \label{lem:symbol-diff-reciprocal-two}
  \uses{lem:symbol-diff-reciprocal-one, lem:symbol-diff-shat}
  \lean{RBM.invSymbolMultiplier_second_diff,
        RBM.invSymbolMultiplier_second_diff_e1,
        RBM.invSymbolMultiplier_second_diff_e2}\leanok
  The exact second forward difference is the second difference of
  $\widehat S$ divided by two denominators plus a quadratic product of
  first differences divided by three denominators. Both coordinate
  instances are proved; their annular norm bounds remain open.
  \emph{T21b algebraic checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Apply the first reciprocal difference formula twice and collect terms.
\end{proof}

\begin{lemma}[Second reciprocal-difference bounds with explicit denominators]
  \label{lem:symbol-diff-reciprocal-two-bound}
  \uses{lem:symbol-diff-reciprocal-two, lem:symbol-diff-shat}
  \lean{RBM.reciprocalSymbolStepBound,
        RBM.reciprocalSymbolSecondBound,
        RBM.norm_invSymbolMultiplier_second_diff_e1_le,
        RBM.norm_invSymbolMultiplier_second_diff_e2_le}\leanok
  The two coordinate second differences obey bounds in terms of the proved
  numerator increments and the exact two- and three-denominator products.
  Annular lower bounds for those denominators remain open.
  \emph{T21c, closed.}
\end{lemma}
\begin{proof}\leanok
  Apply the exact second quotient and triangle inequality, then insert the
  coordinate increment estimates for $\widehat S$.
\end{proof}

\begin{lemma}[Inverse-symbol denominator on a dyadic annulus]
  \label{lem:symbol-denom-annulus}
  \uses{lem:elliptic, lem:cutoff}
  \lean{RBM.norm_one_sub_mul_Shat_ge_dyadic,
        RBM.norm_one_sub_mul_Shat_ge_of_dyadicCutoff_ne_zero}\leanok
  Under the inner-radius condition, the actual multiplier denominator obeys
  $\|1-\xi\widehat S(p)\|\ge
  (4/(45\pi^2))(\kappa(\xi)^2+2^{-2(j+1)})$.
  Nonzero dyadic cutoff supplies that radius condition; the momentum may
  already be shifted. Comparing all shifted momenta in a high-order
  difference remains open. \emph{T21d, closed.}
\end{lemma}
\begin{proof}\leanok
  Apply the established elliptic lower bound and use the cutoff support's
  inner radius.
\end{proof}

\begin{lemma}[Shifted momenta retain a dyadic denominator scale]
  \label{lem:symbol-shift-annulus}
  \uses{lem:symbol-denom-annulus, lem:symbol-diff-reciprocal-two-bound}
  \lean{RBM.pstar2_shift_ge_quarter,
        RBM.norm_one_sub_mul_Shat_ge_e1_shifts,
        RBM.norm_one_sub_mul_Shat_ge_e2_shifts}\leanok
  If the original momentum is sufficiently farther than two lattice steps
  from the origin, one- and two-step coordinate shifts retain at least a
  quarter of its squared radial scale. Under an explicit dyadic inner margin,
  both shifted inverse-symbol denominators have the same dyadic lower bound.
  Relating this margin to all cutoff shells, especially the lowest ones,
  remains open. \emph{T21e, closed.}
\end{lemma}
\begin{proof}\leanok
  Compare each coordinate's periodic angular distance under one or two steps,
  then apply the elliptic denominator bound at the shifted points.
\end{proof}

\begin{lemma}[Second reciprocal difference on a separated annulus]
  \label{lem:symbol-reciprocal-annulus-bound}
  \uses{lem:symbol-shift-annulus,
         lem:symbol-diff-reciprocal-two-bound}
  \lean{RBM.symbolAnnulusDenomScale_pos,
        RBM.norm_invSymbolMultiplier_second_diff_e1_annulus_le,
        RBM.norm_invSymbolMultiplier_second_diff_e2_annulus_le}\leanok
  Under explicit inner-margin assumptions, each coordinate second reciprocal
  difference is bounded by a numerator over $D^2$ plus a quadratic numerator
  over $D^3$, where
  $D=(4/(45\pi^2))(\kappa(\xi)^2+2^{-2(j+1)})$.
  The hypotheses are nonvacuous, but cutoff support and the lowest-frequency
  shells still need separate treatment. \emph{T21f, closed.}
\end{lemma}
\begin{proof}\leanok
  Substitute the three shifted denominator lower bounds into the exact
  reciprocal second-difference norm estimate.
\end{proof}

\begin{lemma}[Dyadic estimate, \texttt{(eq\_dyadic)}]\label{lem:dyadic}
  \uses{lem:cutoff, lem:abel, lem:symbol-diff}
  Assemble the three ingredients into the per-annulus estimate and produce
  a witness of \texttt{DyadicDecomp}.
  \emph{Work order T22.}
\end{lemma}

\begin{lemma}[Property 6, \texttt{(prop:BD1)}--\texttt{(prop:BD2)}]\label{lem:propTH-6}
  \uses{lem:propTH-6-conditional, lem:dyadic}
  The unconditional first- and second-difference bounds of Lemma
  \texttt{lem\_propTH}; T22 must construct the dyadic decomposition with one
  scale-independent constant.
\end{lemma}

\begin{lemma}[Property 6 conditional on the dyadic decomposition]
  \label{lem:propTH-6-conditional}
  \uses{lem:bd-case2, lem:dyadic-case1, lem:kappa-basic}
  \lean{RBM.norm_Theta_fd_all_le_paper,
        RBM.derivativePrefactor_detDom_one,
        RBM.norm_Theta_fd_all_le_rpow}\leanok
  Given an actual \texttt{DyadicDecomp} witness $H$ with $H.C\le C_0$,
  Case 1 and Case 2 imply both paper-scale inequalities for arbitrary
  matrix indices, with a common coefficient
  $8C_0+720(1+\log L)\prec1$.  \emph{Work order T23, conditional core closed.}
\end{lemma}
\begin{proof}\leanok
  Split at $2|s|_L\le|a-b|_L$, combine the two case bounds, and use
  translation invariance to restore arbitrary matrix indices.  The dyadic
  witness and its uniform constant remain obligations of T22.
\end{proof}

\chapter{Delocalization}

\begin{lemma}[Deterministic core of Theorem \texttt{MR:decol}]\label{lem:deloc}
  \lean{RBM.green, RBM.green_eq_spectral, RBM.im_green_apply_self,
    RBM.sq_norm_eigenvector_le_im_green, RBM.sq_norm_eigenvector_le_of_norm_green_le}\leanok
  For Hermitian $H$ with eigenpairs $(\lambda_k,\psi_k)$ and $\eta>0$,
  \[ |\psi_k(x)|^2\;\le\;\sum_l\frac{\eta^2|\psi_l(x)|^2}{(\lambda_k-\lambda_l)^2+\eta^2}
     \;=\;\eta\,\operatorname{Im}G_{xx}(\lambda_k+\mathrm{i}\eta), \]
  so a bound $|G_{xx}|\le C$ gives $|\psi_k(x)|^2\le C\eta$.  The probabilistic
  input (the local law \texttt{MR:locSC}) is a hypothesis here.
\end{lemma}
\begin{proof}\leanok
  In the middle sum the $l=k$ term is $|\psi_k(x)|^2$ and every other term is
  non-negative, which gives the inequality; the spectral decomposition of
  $G(\lambda_k+\mathrm{i}\eta)$ identifies the sum with
  $\eta\operatorname{Im}G_{xx}$.
\end{proof}

\chapter{Stochastic layer: the It\^o replacement stack}

The \S5--\S7 loop estimates can use one-time Gaussian marginals in place of
matrix stochastic calculus.  An audit of these sections
(\texttt{docs/random-layer.md}) found that they do not use a
filtration, the Markov property, a two-time joint law, or Doob: the single
occurrence of ``filtration'' is a quantifier in the hypothesis of Lemma~5.5 and
its proof never touches it.  The whole of \S5--\S7 therefore goes through with
one-time marginals only, and the paper itself already writes
$G_s=(\sqrt sH-z_s)^{-1}$.

\begin{definition}[Two-dimensional block model, \S2.1 and \texttt{(Def\_matE)}]
  \label{def:block-model}
  \uses{def:SB}
  \lean{RBM.Svar, RBM.Svar_apply, RBM.sum_Svar_row, RBM.Svar_conjTranspose,
        RBM.split_bijective, RBM.Spaper_eq, RBM.sum_Spaper_row,
        RBM.Epaper_eq, RBM.Epaper_conjTranspose, RBM.sum_Epaper}
  \leanok
  $S=\SB\otimes S_W$ on $\mathbb Z_{WL}^2$, the blocks $I^{(2)}_a$ and the matrices
  $E_a$ from the paper.  In dimension two, $(S_W)_{ij}=W^{-2}$ and
  $(E_a)_{xy}=\delta_{xy}W^{-2}\mathbf 1(x\in I_a^{(2)})$.
  \emph{Work order T7.}
\end{definition}

\begin{definition}[Stochastic domination]\label{def:stochdom}
  \lean{RBM.StochDom, RBM.StochDomAt, RBM.NormStochDom,
        RBM.DepNormStochDom, RBM.HighProb, RBM.HighProbIn,
        RBM.StochDom.of_forall_le, RBM.StochDomAt.of_unifDetDom_L_scale}
  \leanok
  Definition \texttt{stoch\_domination} (i) itself, as a statement about a
  measure: for every $\tau,D>0$ the bad set has probability $\le N^{-D}$
  eventually.  The deterministic \texttt{def:detdom} already in this blueprint is
  its pointwise shadow.  \emph{Work order S0.}
\end{definition}

\begin{definition}[The one-time Gaussian matrix model]\label{def:model}
  \uses{def:block-model, def:stochdom}
  \lean{RBM.Gauss.P, RBM.Gauss.Xmat, RBM.Gauss.Hflow,
        RBM.Gauss.Xmat_isHermitian, RBM.Gauss.integral_normSq_Xentry,
        RBM.Gauss.Xmat_eq_sum_coordinates, RBM.Gauss.Xmat_update,
        RBM.Gauss.Sizes.seqP, RBM.Gauss.Sizes.seqP_map_slice,
        RBM.Gauss.Sizes.integral_normSq_seqHflow}\leanok
  A finite product of independent real Gaussian coordinates defines the
  Hermitian band matrix $X$, with diagonal variance $S_{ii}$ and real/imaginary
  off-diagonal variances $S_{ij}/2$.  The flow $H_u:=\sqrt u\,X$ has the
  paper's one-time marginal.  A countable product over admissible sizes gives
  a common probability space whose size slices have exactly the fixed-size
  laws, as required by \texttt{StochDomAt}.  Coordinate updates and the
  covariance $\mathbb E|H_{u,ij}|^2=uS_{ij}$ are proved.
  \emph{Work order S4, closed.}
\end{definition}

\begin{lemma}[The deterministic envelope]\label{lem:envelope}
  \uses{def:Theta}
  \lean{RBM.Gauss.norm_green_le, RBM.Gauss.norm_iteratedDeriv_green_le,
        RBM.Gauss.abs_im_mul_norm_le_norm_sub_smul_one,
        RBM.Gauss.iteratedDeriv_lineInverse, RBM.Gauss.norm_lineInversePow_le}\leanok
  For Hermitian $H$ and $|\Im z|\ge\eta>0$, $\|(H-z)^{-1}\|\le\eta^{-1}$, and the
  $k$-th derivative along a line is bounded by $k!\,\|A\|^k\eta^{-(k+1)}$.
\end{lemma}
\begin{proof}\leanok
  $|\Im z|\,\|v\|\le\|(H-z)v\|$ by Cauchy--Schwarz against $v$, using that
  $\langle v,Hv\rangle$ is real.  The derivative bound follows from
  $\frac{d}{dt}(M+tA)^{-1}=-RAR$ iterated, giving $(-1)^kk!\,(RA)^kR$.
  What matters downstream is that both hold on the \emph{whole space}, not on a
  good event: that is what lets the reverse bridge dominate the bad event with no
  mollifying cutoff.  \emph{Work order S1, closed.}
\end{proof}

\begin{lemma}[Scalar Gaussian Stein identities]\label{lem:stein-scalar}
  \lean{RBM.integral_mul_gaussianPDF, RBM.integral_mul_gaussianReal,
        RBM.integral_mul_gaussianReal_of_bdd,
        RBM.integral_mul_gaussianReal_complex}\leanok
  For positive variance, integration by parts against the real Gaussian density
  gives $\mathbb E[Xf(X)]=v\mathbb E[f'(X)]$, first for real and then for
  complex-valued bounded differentiable functions.  \emph{Work order S2, closed.}
\end{lemma}
\begin{proof}\leanok
  Differentiate the Gaussian density, integrate the product derivative over
  the whole real line, then use real and imaginary parts for complex tests.
\end{proof}

\begin{lemma}[Product Gaussian Stein identity]\label{lem:stein-product}
  \uses{lem:stein-scalar}
  \lean{RBM.Gauss.GaussianProduct.map_update,
        RBM.Gauss.GaussianProduct.stein}\leanok
  Replacing one real coordinate of a countable product Gaussian law by an
  independent copy preserves the law.  For bounded continuously differentiable
  complex tests, this yields the coordinatewise Stein identity, including
  zero variance.  \emph{Work order S3, generic core closed.}
\end{lemma}
\begin{proof}\leanok
  Check the product law on measurable cylinders, apply the scalar identity
  along each resampled fibre, and integrate over the remaining coordinates.
\end{proof}

\begin{lemma}[Matrix Gaussian Stein identity]\label{lem:stein}
  \uses{lem:stein-product, def:model}
  \lean{RBM.Gauss.P_map_update, RBM.Gauss.Hflow_update,
        RBM.Gauss.stein_Xmat, RBM.Gauss.stein_Hflow,
        RBM.Gauss.stein_diagonal_sin_example}\leanok
  The product Stein identity applies to the actual Hermitian matrix $X$ and
  $H_u=\sqrt uX$, with the correct real coordinate variance and Hermitian
  direction.  The derivative and boundedness assumptions are explicit; a
  nonconstant diagonal sine test at variance $1/5$ proves satisfiability.
  \emph{S3--S4 interface closed.}
\end{lemma}
\begin{proof}\leanok
  Identify the concrete coordinate law with the Gaussian product law, then
  use the exact matrix update formula to read the fibre derivative as a
  directional matrix derivative.
\end{proof}

\begin{lemma}[Continuous induction]\label{lem:bootstrap}
  \lean{RBM.le_of_bootstrap, RBM.le_of_bootstrap_prefix,
        RBM.le_of_bootstrap_two_mul, RBM.bootstrap_nonconstant_example}\leanok
  If $\varphi$ is continuous and $\varphi\le C\Rightarrow\varphi\le B$, then
  $\varphi\le B$.  This replaces the stopping time of the paper's Step~2, and
  with it the only place where a stopping time enters a \emph{statement}
  (equation \texttt{(105)}, which is then simply not stated).
  The concrete moment functional's continuity and self-improvement are still
  needed downstream. \emph{Work order S8, closed.}
\end{lemma}
\begin{proof}\leanok
  The improved sublevel set is both open and closed in the time interval.
  A supremum argument also proves the prefix form used in Step~2.
\end{proof}

\begin{lemma}[The generator identity]\label{lem:generator}
  \uses{lem:envelope, lem:stein, def:model}
  $\partial_u\,\mathbb E[\Phi(H_u)]=\tfrac12\sum_{ij}S_{ij}\,\mathbb E[\partial_{ij}\partial_{ji}\Phi(H_u)]$.
  Differentiate under the integral, then apply Stein once per coordinate; the
  $\sqrt u$ it produces cancels the $(2\sqrt u)^{-1}$ from the chain rule.  Past
  this node It\^o is off the critical path.  \emph{Work order S5.}
\end{lemma}

\begin{lemma}[Bridge: moments $\Rightarrow\prec$]\label{lem:bridge-prec}
  \uses{def:stochdom}
  \lean{RBM.Gauss.meas_gt_le_of_moment,
        RBM.Gauss.stochDom_of_momentDom,
        RBM.Gauss.stochDomAt_of_momentDomAt,
        RBM.Gauss.stochDom_Icc_of_holder_on_good,
        RBM.Gauss.stochDomAt_Icc_of_holder_on_good}\leanok
  Markov's inequality, with the $\varepsilon$ quantifier outside $p$ so that the
  conclusion holds for every $\tau>0$.  A polynomial time net reduces the
  uncountable union of \texttt{def:stochdom} to finitely many points; between
  points a H\"older bound is required only on a high-probability good event.
  Both ordinary $N$ and admissible physical-size formulations are proved.
  \emph{Work order S6, closed.}
\end{lemma}
\begin{proof}\leanok
  Apply Markov's inequality to a sufficiently high even moment, union bound
  over polynomially many parameters, and absorb the net error with the
  good-event H\"older modulus.  The actual flow's random modulus is a
  downstream input; it is not assumed bounded on every sample.
\end{proof}

\begin{lemma}[Bridge: $\prec\Rightarrow$ moments]\label{lem:bridge-moment}
  \uses{def:stochdom, lem:envelope}
  \lean{RBM.Gauss.integrable_abs_evenPow_of_envelope,
        RBM.Gauss.momentDom_of_stochDom,
        RBM.Gauss.momentDomAt_of_stochDomAt,
        RBM.Gauss.momentDom_green_of_stochDom}\leanok
  Under a global polynomial envelope and a polynomial lower bound on the
  positive scale, stochastic domination gives all even-moment bounds. The
  bad-event contribution uses the global envelope; the Green-function version
  uses the established resolvent bound. \emph{Work order S7, closed.}
\end{lemma}
\begin{proof}\leanok
  Split the moment integral into the high-probability good event and its
  complement; choose the domination exponent to absorb the polynomial
  envelope on the latter.
\end{proof}

\begin{lemma}[A generic Gr\"onwall comparison]\label{lem:gronwall-base}
  \lean{RBM.Gauss.le_gronwallBound_of_hasDerivWithinAt_le,
        RBM.Gauss.gronwall_linear_example}\leanok
  A continuous real function whose right derivative is bounded by
  $K\varphi+\varepsilon$ lies below the Gr\"onwall comparison function.
  This dimension-free Mathlib bridge is available; whether its exponential
  factor has the scale needed for the paper is checked at the actual moment
  inequality. \emph{S9 auxiliary result.}
\end{lemma}
\begin{proof}\leanok
  Apply Mathlib's one-sided slope form of the Gr\"onwall comparison theorem.
\end{proof}

\begin{lemma}[Scale-preserving analytic moment closure]\label{lem:moment-closing-base}
  \lean{RBM.le_two_mul_add_sqrt_of_sq_le,
        RBM.integral_sqrt_mul_le, RBM.sqrt_le_of_isLUB,
        RBM.sqrt_le_of_integral_le,
        RBM.exists_isLUB_Icc,
        RBM.sqrt_le_of_integral_le_of_bdd,
        RBM.sqrt_le_of_integral_le_of_continuousOn}\leanok
  If $\psi\ge0$ satisfies the integrated inequality
  $\psi(u)\le\psi(s)+2\int_s^u\sqrt{\psi(r)}f(r)\,dr
    +c\int_s^u g(r)\,dr$ on $[s,t]$, with the stated nonnegativity,
  integrability and boundedness hypotheses, then
  $\sqrt{\psi(t)}\le\sqrt{\psi(s)}+2\int_s^t f
    +\sqrt{c\int_s^t g}$.
  A separate wrapper obtains the required interval bound from continuity.
  This keeps the time-integrated scale. \emph{Work order S9b, closed.}
\end{lemma}
\begin{proof}\leanok
  Bound the mixed integral using the supremum of $\psi$, then solve the
  resulting quadratic inequality for its square root.
\end{proof}

\begin{lemma}[Time continuity of bounded moments]\label{lem:moment-time-cont}
  \lean{RBM.Gauss.continuousOn_integral_abs_pow_of_envelope,
        RBM.Gauss.continuousOn_regularized_momentRoot_of_envelope}\leanok
  A whole-space deterministic envelope, measurable time slices and continuous
  sample paths imply continuity of every absolute moment and its positive
  regularized root. The finite bare loop now has these inputs under a uniform
  nonreal spectral window; convolved observables still require work.
  \emph{S9c analytic interface, closed.}
\end{lemma}
\begin{proof}\leanok
  Apply dominated convergence with the fixed polynomial envelope and then
  continuity of positive real powers.
\end{proof}

\begin{lemma}[Samplewise time continuity of the matrix flow]
  \label{lem:flow-time-cont}
  \uses{def:model}
  \lean{RBM.Gauss.continuous_Hflow_time,
        RBM.Gauss.continuous_Hflow_entry_time,
        RBM.Gauss.Sizes.continuous_seqHflow_time}\leanok
  For each Gaussian sample, $u\mapsto H_u=\sqrt u\,X$ is continuous as a
  matrix, also under the common probability-space coupling. Continuity of a
  moving-spectral-parameter resolvent loop is a separate step.
  \emph{S9c model interface, closed.}
\end{lemma}
\begin{proof}\leanok
  Compose continuity of the real square root and complex scalar multiplication
  with the fixed sample matrix.
\end{proof}

\begin{lemma}[Resolvent continuity along Hermitian paths]
  \label{lem:green-time-cont}
  \uses{lem:flow-time-cont, lem:envelope}
  \lean{RBM.Gauss.continuous_green_of_isHermitian,
        RBM.Gauss.continuous_green_of_isHermitian_moving,
        RBM.Gauss.continuous_green_Hflow_moving_time}\leanok
  If a finite Hermitian matrix and the spectral parameter vary continuously
  while the imaginary part stays nonzero, their resolvent varies continuously.
  This applies samplewise to $H_u$; the paper's specific $z_u$ and loop product
  remain to be connected. \emph{S9c resolvent interface, closed.}
\end{lemma}
\begin{proof}\leanok
  At each parameter, the shifted Hermitian matrix is a unit; use continuity of
  matrix inversion locally and compose with the two continuous paths.
\end{proof}

\begin{lemma}[Time and sample continuity of resolvent loops]
  \label{lem:loop-time-cont}
  \uses{def:loops, lem:green-time-cont, def:model}
  \lean{RBM.Gauss.HflowBlock, RBM.Gauss.continuous_gloop_Hflow_time,
        RBM.Gauss.continuous_gloop_HflowBlock_sample,
        RBM.Gauss.measurable_gloop_HflowBlock_sample}\leanok
  Relabel the fine-lattice Gaussian flow by the block-site equivalence.
  For a continuous nonreal spectral path, every well-formed finite loop is
  continuous in time at a fixed sample; at a fixed time it is continuous and
  measurable in the Gaussian sample. \emph{S9d, closed.}
\end{lemma}
\begin{proof}\leanok
  Compose the matrix reindexing with resolvent continuity, multiply the finite
  list of Green and block factors, and take the continuous matrix trace.
\end{proof}

\begin{lemma}[Deterministic envelope for finite Gaussian loops]
  \label{lem:loop-envelope}
  \uses{def:loops, lem:loop-time-cont, lem:envelope}
  \lean{RBM.Gauss.norm_gloop_le_crude,
        RBM.Gauss.norm_gloop_HflowBlock_le_crude_on_Icc}\leanok
  If $0<\eta\le|\operatorname{Im}z|$, a loop of length $n$ obeys
  $|\mathcal L|\le(LW)^2(\eta^{-1}W^{-2})^n$. A uniform lower bound on the
  imaginary part over a time interval gives this same deterministic envelope
  for every sample and time. It suffices for bare-loop dominated convergence;
  the paper's sharper normalization and convolved terms remain open.
  \emph{S9e, closed.}
\end{lemma}
\begin{proof}\leanok
  Bound the finite product in operator norm and its trace by matrix size.
\end{proof}

\begin{lemma}[Sharp envelope for nonempty finite loops]
  \label{lem:loop-envelope-sharp}
  \uses{lem:loop-envelope, def:block-model}
  \lean{RBM.Gauss.sum_blockWeight,
        RBM.Gauss.norm_trace_mul_Eblk_le,
        RBM.Gauss.norm_gloop_le_sharp}\leanok
  For a nonempty $n$-loop and $0<\eta\le|\operatorname{Im}z|$,
  $|\mathcal L|\le\eta^{-n}W^{-2(n-1)}$, with no volume factor $L^2$.
  The final block projection has unit trace weight; bounding the preceding
  factors in operator norm gives the stated normalization.
  \emph{S9h, closed.}
\end{lemma}
\begin{proof}\leanok
  Sum the $W^2$ diagonal block weights to one, then bound the remaining
  Green and block factors by their operator norms.
\end{proof}

\begin{lemma}[Continuity of bare-loop moments on a spectral window]
  \label{lem:loop-moment-cont}
  \uses{lem:moment-time-cont, lem:loop-time-cont, lem:loop-envelope}
  \lean{RBM.Gauss.continuousOn_gloop_HflowBlock_window,
        RBM.Gauss.continuousOn_integral_norm_gloop_pow}\leanok
  On a compact time interval where a continuous spectral path obeys
  $|\operatorname{Im}z_u|\ge\eta>0$, every finite moment
  $u\mapsto\mathbb E|\mathcal L_u|^q$ is continuous. This supplies the
  actual bare-loop case of the dominated-continuity interface; the paper's
  specific spectral path and convolved observables remain to be checked.
  \emph{S9f, closed.}
\end{lemma}
\begin{proof}\leanok
  Clamp the spectral path to the time interval, then use samplewise continuity,
  measurability and the deterministic loop envelope in dominated convergence.
\end{proof}

\begin{lemma}[Bulk spectral path and loop-moment window]
  \label{lem:spectral-window}
  \uses{lem:loop-moment-cont}
  \lean{RBM.Gauss.spectralM, RBM.Gauss.spectralZ,
        RBM.Gauss.spectralZ_window_gap_of_bulk,
        RBM.Gauss.continuousOn_integral_norm_gloop_pow_spectralZ}\leanok
  For $|E|\le2-\kappa$ with $\kappa>0$ and a window ending at $t<1$,
  the explicit bulk path $z_u=E+(1-u)m^{(E)}$ has
  $|\operatorname{Im}z_u|\ge(1-t)\operatorname{Im}m^{(E)}>0$.
  Thus its finite bare-loop moments are continuous on the window.
  The identification of this explicit $m^{(E)}$ with the boundary limit
  used to define it in the paper is still to be formalized.
  \emph{S9g, closed at the explicit bulk formula.}
\end{lemma}
\begin{proof}\leanok
  Use the positive square-root formula for the bulk semicircle transform and
  the minimum of $1-u$ on the window, then apply the loop-moment theorem.
\end{proof}

\begin{lemma}[Algebraic identities for the explicit bulk value]
  \label{lem:spectral-algebra}
  \uses{lem:spectral-window}
  \lean{RBM.Gauss.spectralM_mul,
        RBM.Gauss.spectralM_quadratic,
        RBM.Gauss.norm_spectralM}\leanok
  For $|E|\le2$, the explicit bulk value satisfies
  $(m^{(E)})^2+E m^{(E)}+1=0$ and $|m^{(E)}|=1$.
  This algebraic characterization is separate from the still-open
  upper-half-plane boundary-limit identification. \emph{S9i, closed.}
\end{lemma}
\begin{proof}\leanok
  Square the explicit real and imaginary parts and use
  $\bigl(\sqrt{4-E^2}\bigr)^2=4-E^2$.
\end{proof}

\begin{lemma}[Derivative of the moving spectral parameter]
  \label{lem:spectral-derivative}
  \uses{lem:spectral-window, lem:spectral-algebra}
  \lean{RBM.Gauss.hasDerivAt_spectralZ,
        RBM.Gauss.hasDerivAt_spectralZ_im}\leanok
  For the explicit path $z_u=E+(1-u)m^{(E)}$,
  $z'_u=-m^{(E)}$ and
  $(\operatorname{Im}z_u)'=-\operatorname{Im}m^{(E)}$.
  This is an algebraic chain-rule input for the later Green-flow calculation.
  \emph{S5 spectral step, closed.}
\end{lemma}
\begin{proof}\leanok
  Differentiate the affine path in the real parameter $u$.
\end{proof}

\begin{lemma}[Derivative of the one-sample Gaussian matrix flow]
  \label{lem:flow-derivative}
  \uses{def:model, lem:flow-time-cont}
  \lean{RBM.Gauss.hasDerivAt_Hflow_time,
        RBM.Gauss.hasDerivAt_Hflow_one}\leanok
  At a fixed Gaussian sample and $u>0$, the surrogate one-time flow
  $H_u=\sqrt u\,X$ has derivative $X/(2\sqrt u)$.
  This is a pointwise chain-rule input; the paper's OU generator identity
  remains open. \emph{S5 flow step, closed.}
\end{lemma}
\begin{proof}\leanok
  Differentiate the positive real square root and multiply by the fixed
  sampled matrix.
\end{proof}

\begin{lemma}[Derivative of a moving resolvent along the sample flow]
  \label{lem:green-derivative}
  \uses{lem:flow-derivative, lem:spectral-derivative,
         lem:green-time-cont}
  \lean{RBM.Gauss.hasDerivAt_green_moving,
        RBM.Gauss.hasDerivAt_HflowBlock_time,
        RBM.Gauss.hasDerivAt_green_HflowBlock_spectralZ}\leanok
  For $|E|<2$ and $0<u<1$, at each fixed sample,
  $G'_u=-G_u\bigl(X/(2\sqrt u)+m^{(E)}I\bigr)G_u$.
  The formula is a pointwise chain-rule input and does not assert the
  expected loop generator identity. \emph{S5 Green step, closed.}
\end{lemma}
\begin{proof}\leanok
  Differentiate the inverse matrix at the Hermitian nonreal resolvent and
  insert the derivatives of $H_u$ and $z_u$.
\end{proof}

\begin{lemma}[Samplewise derivative of finite resolvent loops]
  \label{lem:loop-derivative}
  \uses{lem:green-derivative, def:loops}
  \lean{RBM.Gauss.hasDerivAt_Gsig_HflowBlock_spectralZ,
        RBM.Gauss.hasDerivAt_loopWord_HflowBlock_spectralZ,
        RBM.Gauss.hasDerivAt_gloopProd_HflowBlock_spectralZ,
        RBM.Gauss.hasDerivAt_gloop_HflowBlock_spectralZ}\leanok
  At a fixed sample and $0<u<1$, the finite signed Green product and its
  trace have the derivative given by the finite Leibniz sum. A nonconstant
  one-edge loop verifies that the derivative formula is substantive.
  Expected-value and generator identities remain open.
  \emph{S5 loop derivative checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Differentiate each signed Green factor and recurse through the finite list,
  then apply the linear matrix trace.
\end{proof}

\begin{lemma}[Single-coordinate derivative of finite loops]
  \label{lem:loop-coordinate-derivative}
  \uses{lem:loop-derivative, lem:loop-contraction-directions}
  \lean{RBM.Gauss.HflowBlock_update,
        RBM.Gauss.hasDerivAt_green_HflowBlock_update,
        RBM.Gauss.hasDerivAt_coordinateWord,
        RBM.Gauss.hasDerivAt_gloop_update}\leanok
  Perturbing one real Gaussian coordinate differentiates the Green matrix by
  $-G(\sqrt u\,B_c)G$ and differentiates a finite loop by its finite
  Leibniz sum. A one-edge example has nonzero directional derivative.
  Weighted coordinate summation and the expected generator remain open.
  \emph{S5 coordinate-derivative checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Differentiate the affine matrix-coordinate perturbation and apply the
  inverse-matrix rule to each factor of the finite loop word.
\end{proof}

\begin{lemma}[Second derivative of a single-coordinate Green path]
  \label{lem:green-coordinate-second}
  \uses{lem:loop-coordinate-derivative}
  \lean{RBM.Gauss.hasDerivAt_greenCoordinateDerivative,
        RBM.Gauss.hasDerivAt_deriv_green_HflowBlock_update}\leanok
  For a fixed nonreal spectral parameter and one affine Hermitian coordinate
  direction $B=\sqrt u\,B_c$, the second Green derivative is $2GBGBG$.
  The finite-loop second product rule and variance-weighted sum remain open.
  \emph{S5 second Green step, closed.}
\end{lemma}
\begin{proof}\leanok
  Differentiate $-GBG$ by the product rule and insert $G'=-GBG$ twice.
\end{proof}

\begin{lemma}[Second coordinate derivative of finite loops]
  \label{lem:loop-coordinate-second}
  \uses{lem:green-coordinate-second, lem:loop-coordinate-derivative}
  \lean{RBM.Gauss.hasDerivAt_coordinateWordDeriv,
        RBM.Gauss.hasDerivAt_deriv_gloopProd_update,
        RBM.Gauss.hasDerivAt_deriv_gloop_update}\leanok
  A finite loop has the second coordinate derivative obtained by the
  recursive product rule, including both cross terms from differentiating
  distinct Green factors. Integrability and expectation-level generator
  identities remain open. \emph{S5 second-loop checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Differentiate the finite first-derivative word recursively and use the
  signed Green second derivative, then apply the trace.
\end{proof}

\begin{lemma}[Uniform envelopes for loop coordinate derivatives]
  \label{lem:loop-coordinate-derivative-bounds}
  \uses{lem:loop-coordinate-second, lem:loop-envelope}
  \lean{RBM.Gauss.norm_coordinateWordDeriv_le,
        RBM.Gauss.norm_gloop_coordinate_derivatives_le}\leanok
  On a nonreal spectral window, both first and second coordinate derivatives
  of each finite loop have deterministic bounds uniform in the Gaussian
  sample. The bounds depend explicitly on loop length, the gap, time and the
  chosen coordinate direction. Measurability and integration remain separate.
  \emph{S5 derivative-envelope checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Induct on the finite loop word using the Green bounds and the recursive
  first and second product rules, then bound the trace.
\end{proof}

\begin{lemma}[Measurability and integrability of loop coordinate derivatives]
  \label{lem:loop-coordinate-integrable}
  \uses{lem:loop-coordinate-derivative-bounds,
         lem:loop-time-cont}
  \lean{RBM.Gauss.measurable_gloop_coordinate_derivatives_sample,
        RBM.Gauss.integrable_gloop_coordinate_derivatives,
        RBM.Gauss.measurable_integrable_actual_gloop_coordinate_derivatives}\leanok
  At fixed time and nonreal spectral parameter, the actual first and second
  Gaussian-coordinate derivatives of a finite loop are measurable and
  integrable under the product Gaussian law. These are the analytic inputs
  for the single-coordinate Stein identity below.
  \emph{S5 derivative-integrability checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Establish sample continuity of the finite derivative words, then combine
  their deterministic envelopes with the probability measure's finite mass.
\end{proof}

\begin{lemma}[Single-coordinate Stein identity for a finite loop]
  \label{lem:loop-coordinate-stein}
  \uses{lem:loop-coordinate-integrable, lem:stein-product}
  \lean{RBM.Gauss.stein_gloop_HflowBlock}\leanok
  At fixed time and nonreal spectral parameter, Gaussian integration by
  parts identifies the expectation of one coordinate times the finite loop
  with its actual coordinate derivative, multiplied by the exact coordinate
  variance (including zero variance). The finite coordinate sum and time
  generator remain separate. \emph{S5 one-coordinate Stein checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Apply product Gaussian Stein to the loop, using the sample continuity and
  deterministic derivative envelope to discharge the analytic hypotheses.
\end{proof}

\begin{lemma}[Closing the paper's moment inequality]\label{lem:gronwall}
  \uses{lem:generator, lem:moment-closing-base, lem:moment-time-cont,
         lem:flow-time-cont, lem:green-time-cont, lem:loop-time-cont,
         lem:loop-envelope, lem:loop-moment-cont, lem:spectral-window}
  Derive the required moment bound from the specific generator and quadratic
  variation estimates while preserving its $\eta$ scale.  The d=1 proof's
  integrate--supremum--quadratic argument is the analytic bridge; the generic
  Gr\"onwall comparison above has an unsuitable loss if used naively here.
  \emph{Work orders S9b/S9, still open.}
\end{lemma}

\begin{lemma}[Discharging the martingale bound]\label{lem:discharge}
  \uses{lem:generator, lem:gronwall}
  The hinge of the whole route: the second-order term the generator produces on
  $|F|^{2p}$, namely $|F|^{2p-2}\sum_\alpha S_\alpha|\partial_\alpha F|^2$, is
  \emph{verbatim} the quadratic variation on the right of the paper's
  equation~\texttt{(108)}.  The counting checks: $\partial_{H(x,y)}$ cuts one $G$
  edge, $S_{xy}=W^{-2}\SB_{[x][y]}$, and $W^4\cdot W^{-2}=W^2$ is the prefactor
  of \texttt{(106)}.  So the paper's long estimate from \texttt{(125)} onwards is
  kept untouched and only its producer changes.  The one identity the paper never
  writes down --- $\sum_\alpha S_\alpha\,\partial_\alpha L\,\overline{\partial_\alpha L}=(E\otimes E)$
  --- has to be proved here, and is the only genuinely new work in the stack.
  \emph{Work order S10.}
\end{lemma}

\chapter{Loop hierarchy and convolution tree, \S2--\S4}

\begin{definition}[Resolvent loops, \texttt{Def:G\_loop}]\label{def:loops}
  \uses{def:model, def:block-model}
  \lean{RBM.LoopIdx, RBM.LoopIdx.WF, RBM.Gsig,
        RBM.gloopProd, RBM.gloop}\leanok
  For signs $\boldsymbol\sigma$ and blocks $\boldsymbol a$, the loop is the
  trace of the cyclic product of $G_t(\sigma_i)E_{a_i}$.  Its normalization is
  the paper's $W^{-2}$ block normalization. Matching sign and block lists are
  recorded by \texttt{LoopIdx.WF}; random-flow specialization comes later.
\end{definition}

\begin{lemma}[Elementary loop identities and block normalization]
  \label{lem:loop-basic}
  \uses{def:loops, def:block-model}
  \lean{RBM.Gsig_conjTranspose, RBM.gloopProd_cons,
        RBM.gloopProd_append, RBM.gloop_rotate,
        RBM.gloop_two_plus_minus_eq, RBM.sum_gloop_head,
        RBM.gloop_two_plus_minus_entries,
        RBM.gloop_two_plus_minus_nonneg}\leanok
  The sign-flipped Green function is the adjoint for Hermitian $H$; products
  concatenate and the trace is cyclic for well-formed lists. Summing the
  first block insertion gives the exact $W^{-2}$ factor. The two-loop trace
  has its expected $G E_a G^* E_b$ form, an explicit squared-entry sum with
  coefficient $W^{-4}$, and is nonnegative for Hermitian $H$.
  \emph{C1 basic package, closed.}
\end{lemma}
\begin{proof}\leanok
  Use the resolvent adjoint identity, fold over matching lists, cyclicity of
  matrix trace, and the already proved $\sum_a E_a=W^{-2}I$.
\end{proof}

\begin{lemma}[Single-edge cut-and-glue on loop indices]
  \label{lem:loop-cut-one}
  \uses{def:loops, lem:loop-basic}
  \lean{RBM.LoopIdx.cutGlue, RBM.LoopIdx.cutGlue_split,
        RBM.LoopIdx.length_cutGlue, RBM.LoopIdx.WF.cutGlue,
        RBM.gloopProd_cutGlue_split}\leanok
  Inserting a block label at any valid edge duplicates that edge's sign,
  increases length by one and preserves matching sign and label lists. The
  resulting finite matrix word has the two corresponding $GE$ factors.
  Two-edge splitting and generator identities remain open.
  \emph{C2 single-edge checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Split the sign and label lists at the chosen edge, then expand the finite
  product and check the lengths of both lists.
\end{proof}

\begin{lemma}[Two-edge cut-and-glue indices]
  \label{lem:loop-cut-pair}
  \uses{lem:loop-cut-one}
  \lean{RBM.LoopIdx.cutGlueL, RBM.LoopIdx.cutGlueR,
        RBM.LoopIdx.length_cutGlueL, RBM.LoopIdx.length_cutGlueR,
        RBM.LoopIdx.length_cutGlueL_add_length_cutGlueR,
        RBM.LoopIdx.WF.cutGlueL, RBM.LoopIdx.WF.cutGlueR}\leanok
  Cutting two valid edges gives left and right index lists whose total length
  is $n+2$; both preserve the matching of signs and block labels. The matrix
  product semantics and generator correspondence remain open.
  \emph{C2 pair-index checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Partition the finite sign and block-label lists at the two cut positions,
  then count the duplicated entries and verify each side has equal lengths.
\end{proof}

\begin{lemma}[Two-edge cut matrix words and traces]
  \label{lem:loop-cut-pair-word}
  \uses{lem:loop-cut-pair, lem:loop-basic}
  \lean{RBM.LoopIdx.cutGlueL_split,
        RBM.LoopIdx.cutGlueR_split,
        RBM.gloopProd_cutGlueL_split,
        RBM.gloopProd_cutGlueR_split,
        RBM.gloop_cutGlueL_split,
        RBM.gloop_cutGlueR_split}\leanok
  At two valid split positions, the left and right cut indices have explicit
  $GE$ matrix products; taking traces gives the corresponding loops.
  Identifying the generator's derivative contractions with these terms is
  still open. \emph{C2 pair matrix-word checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Split the lists at the two cut sites and expand the finite products, then
  apply the trace definition.
\end{proof}

\begin{lemma}[Basic two-dimensional trace contraction]
  \label{lem:loop-contraction-basic}
  \uses{def:block-model, lem:loop-cut-pair-word}
  \lean{RBM.trace_mul_single_mul_single,
        RBM.trace_mul_Eblk,
        RBM.sum_Svar_diag_mul}\leanok
  Elementary matrix units contract a trace to explicit matrix entries.
  Summing a pair of diagonal entries against the two-dimensional variance
  profile gives the exact $W^2$ block factor multiplying
  $\sum_{a,b}\operatorname{Tr}(AE_a)S^{(B)}_{ab}\operatorname{Tr}(CE_b)$.
  Identifying the Gaussian direction derivatives with cut-loop words remains
  open. \emph{S5 contraction checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Expand the matrix-unit trace, regroup fine sites into $W^2$-site blocks,
  and use the explicit tensor-product variance profile.
\end{proof}

\begin{lemma}[Actual Gaussian coordinate contractions]
  \label{lem:loop-contraction-directions}
  \uses{lem:loop-contraction-basic, def:model}
  \lean{RBM.Gauss.coordinateMatrix_real_eq,
        RBM.Gauss.coordinateMatrix_imag_eq,
        RBM.Gauss.coordinateMatrix_diag_eq,
        RBM.Gauss.weighted_trace_coordinate_offDiag,
        RBM.Gauss.weighted_trace_coordinate_diag}\leanok
  The real and imaginary Hermitian directions at an off-diagonal site pair,
  with their actual Gaussian variances, combine in
  $\operatorname{Tr}(ABCB)$ to give
  $S_{ij}(A_{ii}C_{jj}+A_{jj}C_{ii})$; the diagonal direction gives its
  corresponding full-variance term. Summing all coordinates and identifying
  cut-loop chains remain open. \emph{S5 direction checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Expand the three real coordinate matrices into matrix units and cancel
  the off-diagonal cross terms between real and imaginary directions.
\end{proof}

\begin{lemma}[Sum of the used Gaussian coordinate contractions]
  \label{lem:loop-contraction-sum}
  \uses{lem:loop-contraction-directions, lem:loop-contraction-basic}
  \lean{RBM.Gauss.usedCoords,
        RBM.Gauss.sum_usedCoords_trace,
        RBM.Gauss.sum_Spaper_diag_mul_relabel,
        RBM.Gauss.sum_usedCoords_trace_blocks}\leanok
  Summing the actual real, imaginary and diagonal coordinate directions and
  reindexing fine sites by blocks yields
  $W^2\sum_{a,b}\operatorname{Tr}(A'E_a)S^{(B)}_{ab}
  \operatorname{Tr}(C'E_b)$.
  The unused product coordinates and generator derivative chain remain to be
  checked. \emph{S5 finite-coordinate sum checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Partition the finite coordinate type into oriented off-diagonal pairs and
  diagonals, apply the direction identities and reindex via the block split.
\end{proof}

\begin{lemma}[Unused Gaussian coordinates have zero matrix direction]
  \label{lem:loop-contraction-unused}
  \uses{lem:loop-contraction-sum}
  \lean{RBM.Gauss.coordinateMatrix_zero_of_not_mem_usedCoords,
        RBM.Gauss.sum_allCoords_trace_eq_usedCoords,
        RBM.Gauss.sum_allCoords_trace_blocks}\leanok
  Every product coordinate omitted from the used-coordinate sum has zero
  Hermitian matrix direction. Hence the full Gaussian coordinate contraction
  has the same exact $W^2$ block expression as the used-coordinate sum.
  The derivative and cut-loop chain identification remain open.
  \emph{S5 full-coordinate sum checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Classify the unused diagonal-imaginary and lower-oriented coordinates and
  remove their zero terms from the finite sum.
\end{proof}

\begin{lemma}[Coordinate contractions close into cut loops]
  \label{lem:loop-contraction-cutwords}
  \uses{lem:loop-contraction-unused, lem:loop-cut-pair-word}
  \lean{RBM.cutLeftChain, RBM.cutRightChain,
        RBM.trace_cutLeftChain_Eblk,
        RBM.trace_cutRightChain_Eblk,
        RBM.sum_coordinate_cutChains}\leanok
  At a fixed matrix and spectral parameter, the two open matrix chains in
  a Gaussian coordinate contraction close into the left and right cut loops.
  The complete coordinate sum is exactly $W^2$ times their
  $S^{(B)}$-weighted double block sum. This supplies the covariance step of
  the second-loop derivative; term-by-term assembly remains open.
  \emph{S5 cut-chain checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Close each chain with a block projector, apply the pair cut matrix-word
  identities, then use the full-coordinate contraction theorem.
\end{proof}

\begin{lemma}[Spectral drift insertion as a single-edge cut]
  \label{lem:loop-contraction-drift}
  \uses{lem:loop-cut-one, lem:loop-basic}
  \lean{RBM.sum_gloop_cutGlue_split,
        RBM.trace_scalarDrift_cutGlue_split,
        RBM.neg_trace_scalarDrift_cutGlue_split}\leanok
  At any valid edge, inserting $G_s(mI)G_s$ in the matrix word equals
  $mW^2$ times the sum of the single-edge cut loops over the inserted block.
  The inverse-derivative negative sign is recorded separately. The complete
  generator still needs loop derivative multiplicities and expectation.
  \emph{S5 drift checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Replace $I$ by $W^2\sum_b E_b$, then identify each inserted block word with
  the general-position single-edge cut.
\end{proof}

\begin{lemma}[One ordered two-edge term closes into cut loops]
  \label{lem:loop-contraction-second}
  \uses{lem:loop-coordinate-second, lem:loop-contraction-cutwords}
  \lean{RBM.trace_twoEdge_mixed_eq_cutChains,
        RBM.sum_twoEdge_mixed_cutChains,
        RBM.sum_twoEdge_mixed_coordinate,
        RBM.sum_twoEdge_mixed_deriv}\leanok
  For one ordered pair of distinct Green edges, the Gaussian variance sum
  of the cross term in the second coordinate derivative equals $uW^2$ times
  the $S^{(B)}$-weighted left/right cut-loop double sum. The reverse ordering,
  same-edge terms, and expectation-level assembly remain open.
  \emph{S5 two-edge contraction checkpoint, closed.}
\end{lemma}
\begin{proof}\leanok
  Rotate the trace to expose the two coordinate directions, apply the finite
  covariance contraction, and use $(\sqrt u)^2=u$; the two derivative signs
  cancel.
\end{proof}

\begin{definition}[Cut-and-glue operations, \texttt{Def:oper\_loop}]\label{def:loop-ops}
  \uses{def:loops, lem:loop-cut-one, lem:loop-cut-pair,
         lem:loop-cut-pair-word}
  The one-edge insertion and two-edge splitting operations act on sign and
  block-indexed loops.  Their exact index changes must be verified in d=2.
\end{definition}

\begin{definition}[Convolution tree, \texttt{Def\_Ktza}]\label{def:ktree}
  \uses{def:loop-ops, def:Theta}
  Define $\mathcal K$ by the paper's triangular evolution equation and initial
  data $W^{-2(n-1)}\prod_i m(\sigma_i)\mathbf 1(a_1=\cdots=a_n)$.
  Existence and uniqueness are proof obligations.
\end{definition}

\begin{lemma}[Loop hierarchy, \texttt{lem:SE\_basic}]\label{lem:loop-hierarchy}
  \uses{def:loop-ops, lem:generator}
  Derive the loop evolution identity, including its $W^2$ factor, from the
  one-time Gaussian generator rather than an It\^o SDE.
\end{lemma}

\begin{lemma}[Two-loop tree formula, \texttt{(Kn2sol)}]\label{lem:ktree2}
  \uses{def:ktree, def:Theta}
  For $n=2$, show $\mathcal K=W^{-2}m_1m_2\Theta^{(B)}_{tm_1m_2}$ and verify
  its initial data and evolution equation.
\end{lemma}

\begin{remark}[Results cited from \texttt{[YY\_25]}]\label{rem:yy25}
  The paper invokes, without reproducing their full proofs, the convolution
  tree partition formula and parts of its Ward identity and short-range
  combinatorics from \texttt{[YY\_25]}.  These are explicit open dependencies
  until their precise hypotheses and Lean proofs have been audited.
\end{remark}

\begin{lemma}[Pure-loop decay, \texttt{lem\_pureloop}]\label{lem:pureloop}
  \uses{def:ktree, lem:propTH-5, rem:yy25}
  The all-plus tree decays exponentially in the maximum periodic separation
  of its block indices.
\end{lemma}

\begin{lemma}[Ward identity for loops, \texttt{(WI\_calL)}]\label{lem:ward-loop}
  \uses{def:loops, def:block-model, lem:ward-two}
  \lean{RBM.sum_gloop_ward_last, RBM.sum_gloop_ward_last_div}\leanok
  Sum one block label using $\sum_a E_a=W^{-2}I$ and
  $G(z)G(\bar z)=(G(z)-G(\bar z))/(2\mathrm{i}\eta)$.
  The corrected paper coefficient is $1/(2\mathrm{i}W^2\eta)$.
  For arbitrary well-formed sign and label lists beginning with $+$ and ending
  with $-$, the divided identity holds when $\operatorname{Im}z\ne0$ and both
  resolvents are invertible. \emph{C1 loop Ward, closed.}
\end{lemma}
\begin{proof}\leanok
  Rotate the last insertion to the front, sum its block projector, apply the
  traced resolvent identity, then divide by the nonzero $2\mathrm{i}\eta$.
\end{proof}

\begin{lemma}[Corrected Ward identity for two loops]\label{lem:ward-two}
  \uses{def:loops, lem:loop-basic, def:block-model}
  \lean{RBM.green_sub_green, RBM.green_sub_green_conj',
        RBM.trace_green_sub_trace_green_conj',
        RBM.sum_gloop_two_ward}\leanok
  Under the two explicit resolvent invertibility hypotheses,
  $2\mathrm{i}\operatorname{Im}z\sum_b\mathcal L_{(+,-),(a,b)}
   =W^{-2}\bigl(\operatorname{Tr}(G(z)E_a)
                -\operatorname{Tr}(G(\bar z)E_a)\bigr)$.
  The Hermitian nonreal case satisfies those hypotheses. The tree Ward
  statement remains open. \emph{C1 Ward $n=2$, closed.}
\end{lemma}
\begin{proof}\leanok
  Apply the resolvent identity with $z-\bar z=2\mathrm{i}\operatorname{Im}z$,
  rotate the two-loop trace, and sum its last block projector using
  $\sum_b E_b=W^{-2}I$.
\end{proof}

\begin{lemma}[Ward identity for trees, \texttt{(WI\_calK)}]\label{lem:ward-tree}
  \uses{def:ktree, lem:ktree2, rem:yy25}
  Use the same corrected coefficient $1/(2\mathrm{i}W^2\eta)$.
  Prove the corresponding tree identity, including the $n\ge4$ inductive case.
\end{lemma}

\begin{lemma}[Convolution-tree partition formula, \texttt{(KKpi)}]\label{lem:tree-decomp}
  \uses{def:ktree, rem:yy25}
  Identify the admissible partitions and the $\Sigma^{(\emptyset)}$ factors,
  including their sum-zero, short-range and reflection properties.
\end{lemma}

\begin{lemma}[Tree bounds, \texttt{ML:Kbound} and \texttt{ML:Kbound+pi}]\label{lem:tree-bound}
  \uses{lem:pureloop, lem:ward-tree, lem:tree-decomp,
         lem:propTH-5, lem:propTH-6}
  Bound $\mathcal K$ at the paper's scale
  $M_t^{-n+1}$, where $M_t=W^2\ell_t^2\eta_t$.
\end{lemma}

\chapter{Main probabilistic estimates and results}

\begin{lemma}[Loop induction, \texttt{lem:main\_ind}]\label{lem:loop-induction}
  \uses{lem:loop-hierarchy, lem:tree-bound, lem:bridge-prec,
         lem:bridge-moment, lem:gronwall, lem:discharge, lem:bootstrap}
  Propagate the loop, two-loop and local resolvent estimates over the paper's
  time mesh, replacing the stopping-time step with continuous induction.
\end{lemma}

\begin{lemma}[Loop and local estimates, \texttt{ML:GLoop}, \texttt{ML:GLoop\_expec},
              \texttt{ML:GtLocal}]\label{lem:loop-estimates}
  \uses{lem:loop-induction, lem:tree-bound}
  Establish the hierarchy error, its two-loop spatial refinement, and the
  entrywise local estimate at the paper's $M_t$ scale.
\end{lemma}

\begin{lemma}[Improved two-loop expectation, \texttt{ML:exp}]\label{lem:loop-expectation}
  \uses{lem:loop-estimates, lem:tree-bound}
  Prove the $M_t^{-3}$ expectation improvement needed for quantum unique
  ergodicity.
\end{lemma}

\begin{theorem}[Local semicircle law, \texttt{MR:locSC}]\label{thm:local-law}
  \uses{lem:loop-estimates, def:model, def:stochdom}
  The entrywise and partial-trace local laws hold for
  $|E|\le2-\kappa$ and $N^{-1+\tau}\le\eta\le1$ under $W\ge N^{\mathfrak c}$.
\end{theorem}

\begin{theorem}[Delocalization, \texttt{MR:decol}]\label{thm:delocalization}
  \uses{thm:local-law, lem:deloc}
  Deduce the bulk eigenvector $\ell^\infty$ bound from the local law and the
  already formalized deterministic spectral inequality.
\end{theorem}

\begin{theorem}[Generalized quantum unique ergodicity, \texttt{MR:QUE}]\label{thm:que}
  \uses{lem:loop-expectation, thm:local-law, def:block-model}
  Control the matrix elements of $E_a-N^{-1}I$ between bulk eigenvectors and
  the associated mass on unions of blocks.
\end{theorem}

\begin{theorem}[Quantum diffusion, \texttt{MR:QDiff}]\label{thm:qdiff}
  \uses{lem:loop-estimates, lem:ktree2, thm:local-law}
  Compare the two resolvent two-loop traces to their block propagator profiles.
\end{theorem}

\begin{remark}[External inputs to bulk universality]\label{rem:universality-inputs}
  The proof of \texttt{Thm: B\_Univ} uses the short-time Dyson Brownian motion
  theorem cited as \texttt{[LANDON20191137]}, correlation-function comparison
  estimates cited as \texttt{[erdHos2017dynamical]} and \texttt{[Xu:2024aa]},
  and several arguments cited from
  \texttt{[YY\_25]}.  Their exact statements and Lean proofs remain open
  dependencies; citing them does not close the theorem below.
\end{remark}

\begin{lemma}[Short-time comparison, \texttt{(417)}]\label{lem:short-time-comparison}
  \uses{thm:local-law, thm:delocalization, thm:que,
         rem:universality-inputs}
  For the matrix Ornstein--Uhlenbeck flow over time
  $t_*=N^{-1+\tau_U}$, control the change in expectations of products of
  normalized resolvent traces at the paper's microscopic spectral scales.
\end{lemma}

\begin{theorem}[Bulk universality, \texttt{Thm: B\_Univ}]\label{thm:bulk-univ}
  \uses{lem:short-time-comparison, rem:universality-inputs}
  For each fixed $k$, bulk energy $E$, and compactly supported smooth test
  function, the rescaled $k$-point correlation functions of the band matrix
  and GUE have the same limiting test integral.
\end{theorem}
